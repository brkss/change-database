DROP TABLE IF EXISTS `admins`;
CREATE TABLE `admins` (
    `id` LONG,
    `username` VAR_STRING,
    `password` BLOB,
    `tokenVersion` LONG,
);

INSERT INTO `admins` VALUES ();
INSERT INTO `admins` VALUES ();
